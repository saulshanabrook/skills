interface:
  display_name: "Sociocracy"
  short_description: "Coordinate long-running work with bounded agents"
  default_prompt: "Use $sociocracy to coordinate this long-running task with a clear mission, bounded sub-agent domains, evidence handoffs, review loops, and stop rules."
